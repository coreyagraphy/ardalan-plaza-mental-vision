import type { PlazaMats } from "./materials";

/** Former detached vignette — foyer now owns the interior. Kept so imports don't break. */
export function GalleryVignette(_props: { mats: PlazaMats; dusk: boolean }) {
  return null;
}
