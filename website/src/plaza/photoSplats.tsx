/**
 * Photo-projected Gaussians are retired.
 * They smeared the official Studio M plate onto the wrong villa mesh.
 * Source camera now shows the published exterior as a 2D plate overlay.
 */

export function detectSplatEngine(_gl?: unknown): "off" {
  return "off";
}

export function PhotoSplatCloud() {
  return null;
}
